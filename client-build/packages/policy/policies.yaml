rules:
  - name: logical_invariants
    when: "metrics.coherence_tech < 0.8"
    then: block

  - name: narrative_contradiction
    when: "metrics.nli_contradiction > 0.2"
    then: revise

  - name: ethics_guard
    when: "metrics.ethics_risk >= 0.5"
    then: block
