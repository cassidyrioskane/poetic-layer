version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - mapping-service

  mapping-service:
    build:
      context: .
      dockerfile: services/mapping-service/Dockerfile
    ports:
      - "8000:8000"
